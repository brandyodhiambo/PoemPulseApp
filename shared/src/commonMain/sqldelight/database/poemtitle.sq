import kotlin.Int;

CREATE TABLE poemTitleEntity (
    id INTEGER AS Int NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL
);

getAllPoemTitle:
SELECT * FROM poemTitleEntity;

deletePoemTitle:
DELETE FROM poemTitleEntity;

insertPoemTitle:
INSERT OR REPLACE
INTO poemTitleEntity(title)
VALUES (?);